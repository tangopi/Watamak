<Page
    x:Class="Watamak.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Watamak"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <canvas:CanvasControl x:Name="canvasOfAvaga" ClearColor="CornflowerBlue" />
        <Button Grid.Row="1" HorizontalAlignment="Stretch" x:Name="BrowseBaseButton">Browse Base</Button>
        <Button Grid.Row="2" HorizontalAlignment="Stretch" x:Name="BrowseWatermark">Browse Watermark</Button>
        <Button Grid.Row="3" HorizontalAlignment="Stretch" x:Name="SaveWatermark">Save Wachamak</Button>
    </Grid>
</Page>
